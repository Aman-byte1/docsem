# CPU-only OCR, used for local development on machines without a GPU.
# On RunPod we OCR with Qwen2.5-VL instead (higher quality) and do not need this.
rapidocr-onnxruntime>=1.3
opencv-python-headless>=4.9